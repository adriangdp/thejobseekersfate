export enum enumJobStatus {
    offer = "offer", 
    applied = "applied", 
    rejected = "rejected", 
    interview = "interview", 
    ghosted = "ghosted",
    accepted = "accepted"
};

export enum enumSortModes {
    default = "default",
    salary = "salary",
    status = "status",
    date = "date"
}